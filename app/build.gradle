apply plugin: 'com.android.application'

android {
    namespace 'com.example.imguiandroid'
    compileSdk 34

    defaultConfig {
        applicationId "com.example.imguiandroid"
        minSdk 21
        targetSdk 34
        versionCode 1
        versionName "1.0"

        // NDK version pre-installed on GitHub Actions ubuntu-latest runners
        ndkVersion "28.2.13676358"

        externalNativeBuild {
            cmake {
                cppFlags "-std=c++17"
                arguments "-DANDROID_STL=c++_shared"
            }
        }

        ndk {
            abiFilters 'armeabi-v7a', 'arm64-v8a', 'x86', 'x86_64'
        }
    }

    externalNativeBuild {
        cmake {
            path "src/main/cpp/CMakeLists.txt"
            // No version pin: Gradle picks the highest CMake available in the SDK
        }
    }

    buildTypes {
        debug {
            debuggable true
            jniDebuggable true
        }
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }

    sourceSets {
        main {
            assets.srcDirs = ['src/main/assets']
            manifest.srcFile 'AndroidManifest.xml'
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
}

dependencies {
    // NativeActivity apps have no Java dependencies
}
